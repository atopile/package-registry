# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  hosting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ATOPILE }}'
          channelId: live
          projectId: atopile
  functions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jsryudev/deploy-firebase-functions@c1c16fa12e21b0dc7db1284ef928d7b058c06a79
        env:
          FIREBASE_TOKEN: '${{ secrets.GITHUB_TOKEN }}'
          FIREBASE_PROJECT: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ATOPILE }}'
